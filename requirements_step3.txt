# Requirements for Step 3 Enhanced Challenge-Response System
# This file contains all dependencies needed to run the Step 3 implementation

# ============================================================================
# CORE COMPUTER VISION & AI LIBRARIES (ESSENTIAL)
# ============================================================================
# PyTorch for deep learning (CPU version for compatibility)
torch>=2.0.0 --index-url https://download.pytorch.org/whl/cpu
torchvision>=0.15.0 --index-url https://download.pytorch.org/whl/cpu

# OpenCV for computer vision and camera access
opencv-python>=4.8.0

# MediaPipe for facial landmark detection
mediapipe>=0.10.0

# NumPy for numerical operations
numpy>=1.24.0,<2.0.0

# Pillow for image processing
Pillow>=10.0.0

# ============================================================================
# STEP 3 SPECIFIC AUDIO DEPENDENCIES (ESSENTIAL FOR STEP 3)
# ============================================================================
# pygame for audio feedback (success/failure sounds)
pygame>=2.5.0

# pyttsx3 for text-to-speech voice instructions
pyttsx3>=2.90

# ============================================================================
# WEB FRAMEWORK DEPENDENCIES (FOR WEB INTERFACE)
# ============================================================================
# Flask for web application
flask>=2.3.0
werkzeug>=2.3.0

# Flask-SocketIO for real-time communication
flask-socketio>=5.3.0

# ============================================================================
# SCIENTIFIC & UTILITY LIBRARIES
# ============================================================================
# scikit-learn for machine learning utilities
scikit-learn>=1.3.0

# matplotlib for visualization (optional but useful for debugging)
matplotlib>=3.7.0

# tqdm for progress bars
tqdm>=4.65.0

# ============================================================================
# FACE RECOGNITION (IF USING FACE RECOGNITION FEATURES)
# ============================================================================
# Note: face-recognition requires dlib which can be problematic
# Install only if you need face recognition features
# face-recognition>=1.3.0

# ============================================================================
# PYTHON BUILT-IN MODULES (NO INSTALLATION NEEDED)
# ============================================================================
# These are already included with Python:
# - sqlite3 (database)
# - json (data parsing)
# - time (timing functions)
# - threading (multi-threading)
# - queue (thread communication)
# - collections (data structures)
# - enum (enumerations)
# - pathlib (file paths)
# - os, sys (system operations)
# - typing (type hints)
# - logging (logging functionality)
